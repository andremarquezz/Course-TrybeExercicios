type Point = {
  perna: string;
  rosto: string;
  cara: string;
  pulmao: string;
};

function printCoord(pt: Point) {
  console.log('O valor da cordenada x é: ' + pt.rosto);
  console.log('O valor da coordenada y é: ' + pt.cara);
}
